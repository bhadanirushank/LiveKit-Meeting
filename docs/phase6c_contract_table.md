### Phase 6C Endpoint Contract Table

| Endpoint | Auth Type | Required Headers | Request Body | Response Body | Success Status | Error / Problem Details Codes | Idempotency | Token Consumption | Rate Limits | Expiry Behavior |
|----------|-----------|-------------------|--------------|---------------|----------------|-------------------------------|-------------|-------------------|-------------|-----------------|
| `POST /api/v1/host-sessions/exchange` | None | `X-Installation-Id`, `Idempotency-Key` (defaults used if missing) | `{"publicMeetingCode": "str", "hostSecret": "str"}` | `{"accessToken": "str", "refreshToken": "str"}` | 200 OK | `400` (Invalid input), `401` (Invalid secret/code), `409` (`IDEMPOTENCY_KEY_REUSED`, `CONCURRENT_REQUEST_PROCESSING`) | **Yes** (via `IdempotencyService` hash, TTL 24h) | N/A | None | N/A |
| `GET /api/v1/meetings/{meetingCode}/waiting-room` | Host Bearer | None | None | `{"requests": [{"id": "uuid", "displayName": "str", "requestedAt": "timestamp"}]}` | 200 OK | `400` (Missing param), `404` (Unknown error) | None | N/A | None | N/A |
| `POST /api/v1/meetings/{meetingCode}/waiting-room/{requestId}/admit` | Host Bearer | `X-Installation-Id`, `Idempotency-Key` | None | `{"status": "ADMITTED"}` | 200 OK | `400` (Missing param), `404` (`ADMIT_FAILED`), `409` (`CAPACITY_REACHED` or `IDEMPOTENCY_KEY_REUSED`) | **Yes** (`IdempotencyService`) | N/A | None | N/A |
| `POST /api/v1/meetings/{meetingCode}/waiting-room/{requestId}/reject` | Host Bearer | None | `{"reason": "str?"}` | `{"status": "REJECTED"}` | 200 OK | `400` (Missing param), `404` (Unknown error) | None | N/A | None | N/A |
| `GET /api/v1/join-requests/{requestId}` | Mobile Bearer | None | None | `{"status": "PENDING\|ADMITTED\|REJECTED\|EXPIRED"}` | 200 OK | `400` (`INVALID_REQUEST`), `401` (Unauthorized), `403` (`JOIN_REQUEST_NOT_OWNED`, `PARTICIPANT_REJECTED`), `404` (`JOIN_REQUEST_NOT_FOUND`), `410` (`JOIN_REQUEST_EXPIRED`) | None | **No**. Polling this endpoint is safe and does not consume tokens. | None | Status `EXPIRED` or `expiresAt` < now returns 410 Gone. |
| `POST /api/v1/join-requests/{requestId}/livekit-token` | Mobile Bearer | `Idempotency-Key` (mandatory, 16-128 chars regex validated) | None | `{"token": "raw_token"}` OR `{"status": "WAITING_ROOM"}` | 200 OK or 202 Accepted | `400` (`INVALID_REQUEST`, `INVALID_IDEMPOTENCY_KEY`), `401` (Unauthorized), `403` (`JOIN_REQUEST_NOT_OWNED`, `JOIN_REQUEST_EXPIRED`, `MEETING_ENDED`, `MEETING_LOCKED`, `PARTICIPANT_REJECTED`, `FORBIDDEN`), `404` (`JOIN_REQUEST_NOT_FOUND`), `409` (`TOKEN_ALREADY_ISSUED`, `MEETING_CAPACITY_REACHED`), `410` (`REPLAY_EXPIRED`) | **Yes**. Strict 60s replay window using identical key. Different key -> `TOKEN_ALREADY_ISSUED` conflict. | **Yes**. Issuing the token atomically consumes the admission authorization natively. | None | Replay window strictly 60s. After 60s, replay returns 410 Gone. |
| `POST /api/v1/meetings/{meetingCode}/start` | Host Bearer | `X-Installation-Id`, `Idempotency-Key` | None | `{"status": "STARTING/LIVE"}` | 200 OK | `400` (`START_FAILED`), `404` (Not Found), `409` (`IDEMPOTENCY_KEY_REUSED`) | **Yes** (`IdempotencyService`) | N/A | None | N/A |
| `POST /api/v1/host-sessions/livekit-token` | Host Bearer | None | None | `{"token": "raw_token"}` | 200 OK | `401` (Unauthorized / missing session), `404` (Meeting Not Found) | None | No. Creates/fetches host participant session and returns fresh token. | None | N/A |
| `GET /api/v1/meetings/{meetingCode}/status` | Any Bearer | None | None | `{"status": "MeetingStatus", "title": "str"}` | 200 OK | `400` (Bad Request), `404` (Meeting Not Found) | None | N/A | None | N/A |
